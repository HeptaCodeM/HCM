package com.hcm.grw.config;
import javax.servlet.http.HttpServletRequest;
import javax.servlet.http.HttpSession;

import org.springframework.stereotype.Service;

import com.raonsecure.touchenkey.*;

@Service
public class E2ECryptoService {
	/*
	public E2ECrypto getE2ECryptoInfo(HttpServletRequest req) {
		String privateKey = new String("C:/Users/jjang/git/HCM/HCM_Project/src/main/webapp/WEB-INF/raon_config/nxkey/Private2048.key.der");	
		HttpSession session = req.getSession();
		return new E2ECrypto(req, session, privateKey);
	}
	*/
}
